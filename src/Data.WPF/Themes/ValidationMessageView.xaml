<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DevZest.Data.Views">

    <Style TargetType="{x:Type local:ValidationMessageView}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ValidationMessageView}">
                    <TextBlock x:Name="_textBlock" Text="{TemplateBinding Description}" FontWeight="Bold" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="Severity" Value="Error">
                            <Setter TargetName="_textBlock" Property="Foreground" Value="Red" />
                        </Trigger>
                        <Trigger Property="Severity" Value="Warning">
                            <Setter TargetName="_textBlock" Property="Foreground" Value="Purple" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
