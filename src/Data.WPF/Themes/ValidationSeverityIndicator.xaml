<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DevZest.Data.Views">

    <Style TargetType="{x:Type local:ValidationSeverityIndicator}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ValidationSeverityIndicator}">
                    <TextBlock x:Name="_textBlock" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="ValidationSeverity" Value="Error">
                            <Setter TargetName="_textBlock" Property="Text" Value="*" />
                        </Trigger>
                        <Trigger Property="ValidationSeverity" Value="Warning">
                            <Setter TargetName="_textBlock" Property="Text" Value="!" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
