<Window x:Class="DevZest.Data.Views.PasteAppendWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:res="clr-namespace:DevZest.Data"
        xmlns:v="clr-namespace:DevZest.Data.Views"
        WindowStartupLocation="Manual"
        WindowStyle="ToolWindow"
        Title="{x:Static res:UserMessages.PasteAppendWindow_Title}"
        Width="600" Height="400">
    <StackPanel Orientation="Vertical" Margin="5">
        <v:DataView x:Name="_dataView" HorizontalAlignment="Stretch" Height="330" BorderThickness="1" BorderBrush="Gray" Margin="0,5,0,0" />
        <UniformGrid HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button IsDefault="True" Content="{x:Static res:UserMessages._OK}" Command="{x:Static v:SortWindow.Apply}" />
            <Button Margin="5,0,0,0" IsCancel="True" Content="{x:Static res:UserMessages._Cancel}" Command="{x:Static v:SortWindow.Cancel}" />
        </UniformGrid>
    </StackPanel>
</Window>
