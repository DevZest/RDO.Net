<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DevZest.Data.Views">

    <ControlTemplate x:Key="{x:Static local:ValidationMessageView+Templates.ValidationError}">
        <StackPanel Orientation="Vertical">
            <Border x:Name="_border" BorderBrush="Red" BorderThickness="2" HorizontalAlignment="Left">
                <AdornedElementPlaceholder />
            </Border>
        </StackPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="{x:Static local:ValidationMessageView+Templates.ValidationWarning}">
        <StackPanel Orientation="Vertical">
            <Border x:Name="_border" BorderBrush="Purple" BorderThickness="2" HorizontalAlignment="Left">
                <AdornedElementPlaceholder />
            </Border>
        </StackPanel>
    </ControlTemplate>
</ResourceDictionary>
