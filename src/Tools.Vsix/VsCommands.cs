namespace DevZest.Data.Tools
{
    // Commands can be found at Environment/Keyboard page of Options dialog box in VS.
    public static class VsCommands
    {
        public static class Edit
        {
            public const string InsertTab = nameof(Edit) + "." + nameof(InsertTab);
            public const string BreakLine = nameof(Edit) + "." + nameof(BreakLine);
        }
    }
}
