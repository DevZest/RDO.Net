using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Linq;

namespace DevZest.Data.Primitives
{
    [TestClass]
    public class ColumnConverterTests
    {
        [TestMethod]
        public void GetTypes()
        {
            //var result = typeof(_Boolean).Assembly.GetTypes().Where(x => x.Name.StartsWith("CaseExpression")).ToArray();
            //var genericType = typeof(CaseExpression<bool?>);
            //Assert.AreEqual(true, genericType.IsGenericType);
            //var genericTypeDef = genericType.GetGenericTypeDefinition();
            //Assert.AreEqual(typeof(CaseExpression<>), genericTypeDef);
        }
    }
}
